CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(ToyGE)

SET(TOYGE_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../..")

LIST(APPEND CMAKE_MODULE_PATH "${TOYGE_ROOT_DIR}/cmake")

INCLUDE(${TOYGE_ROOT_DIR}/cmake/Common.cmake)

SET(TOYGE_OUTPUT_DIR "${TOYGE_ROOT_DIR}/bin/${PLATFORM_TARGET}")

# External packages
FIND_PACKAGE(AntTweakBar REQUIRED)
FIND_PACKAGE(Assimp REQUIRED)
FIND_PACKAGE(Boost REQUIRED)
FIND_PACKAGE(FreeImage REQUIRED)
FIND_PACKAGE(FreeType REQUIRED)
FIND_PACKAGE(DirectXTex REQUIRED)

SET(RAPIDXML_DIR ${TOYGE_ROOT_DIR}/Externals/rapidxml-1.13)
SET(MCPP_DIR ${TOYGE_ROOT_DIR}/Externals/mcpp-2.7.2)

# CoreLibName
SET(TOYGE_CORE_LIB_NAME ToyGE_Core)

SET_PROPERTY(GLOBAL PROPERTY USE_FOLDERS ON)

# CopyDlls


#FILE(COPY ${AntTweakBar_BINARY_RELEASE} DESTINATION COMPILE_DST_DIR)
#FILE(COPY ${Assimp_BINARY_RELEASE} DESTINATION COMPILE_DST_DIR)
#FILE(COPY ${FreeImage_BINARY_RELEASE} DESTINATION COMPILE_DST_DIR)

ADD_SUBDIRECTORY(Core)
ADD_SUBDIRECTORY(Plugins)
ADD_SUBDIRECTORY(Samples)
ADD_SUBDIRECTORY(Tools)